# Base URL for the RAG API.
# Leave empty for local dev — the Vite proxy (vite.config.js) forwards
# /health, /ingest and /ask to the backend, so no CORS is needed.
# For a production build served separately from the API, set this to the
# API origin, e.g. http://localhost:8000
VITE_API_BASE_URL=
